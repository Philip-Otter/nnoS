# nslookup name output stripper (nnoS.py)

This tool is used to remove just the names from an nslookup query. It can be used in conjunction with other tools in which you would just want the name data.
